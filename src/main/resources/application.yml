spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/appdb
    username: appuser
    password: AppPass!123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.example.accounttask: DEBUG



